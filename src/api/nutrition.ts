export interface Meal { id: string; name: string; foods: FoodItem[]; totalCalories: number; totalProtein: number; totalCarbs: number; totalFat: number; timestamp: string; }
export interface FoodItem { name: string; serving: number; unit: string; calories: number; protein: number; carbs: number; fat: number; }
export interface DailyNutrition { date: string; meals: Meal[]; totalCalories: number; totalProtein: number; totalCarbs: number; totalFat: number; waterIntake: number; }
export function calculateMacros(foods: FoodItem[]): { calories: number; protein: number; carbs: number; fat: number } { return foods.reduce((acc, f) => ({ calories: acc.calories + f.calories, protein: acc.protein + f.protein, carbs: acc.carbs + f.carbs, fat: acc.fat + f.fat }), { calories: 0, protein: 0, carbs: 0, fat: 0 }); }
export function suggestMealPlan(goal: 'lose' | 'maintain' | 'bulk', weight: number): Meal[] { const calories = goal === 'lose' ? weight * 22 : goal === 'bulk' ? weight * 33 : weight * 28; return [{ id: 'm1', name: 'Suggested Breakfast', foods: [], totalCalories: Math.round(calories * 0.25), totalProtein: 30, totalCarbs: 60, totalFat: 15, timestamp: new Date().toISOString() }]; }
